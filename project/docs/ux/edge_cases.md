# 用户边界场景（v0.1）

## 输入类
- 文件格式不支持（非 MP3/WAV）。
- 音频时长过长导致超时。
- 文件损坏或读取权限不足。

## 处理类
- 模型加载失败或版本不兼容。
- 推理中断（用户取消、内存不足）。
- 进度卡住但任务未结束。

## 输出类
- 导出路径不可写。
- 导出文件被占用导致失败。
- 目标软件无法打开导出文件。

## 交互类
- 用户重复点击开始按钮。
- 用户在处理中切换模式。
- 用户关闭窗口导致任务中断。

## 应对策略
- 每个错误必须有明确原因与下一步建议。
- 可重试动作应提供一键重试入口。
